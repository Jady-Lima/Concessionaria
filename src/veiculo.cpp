#include <iostream>
#include "veiculo.h"

Veiculo::Veiculo(string ch, string m, double p, int a) : Concessionaria(estoque, nome, CNPJ)
{
    chassi = ch;
    marca = m;
    preco = p;
    ano_fabricacao = a;
}

Veiculo::~Veiculo()
{
}